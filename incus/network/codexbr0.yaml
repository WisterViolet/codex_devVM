description: Isolated bridge for Codex development VM

config:
    ipv4.address: 10.200.200.1/24
    ipv4.nat: "true"

    ipv6.address: none
    security.acls: codex-isolation

    raw.dnsmasq: port=0

security.acls.default.ingress.action: reject
security.acls.default.egress.action: reject
